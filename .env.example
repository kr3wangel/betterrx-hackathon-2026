ANTHROPIC_API_KEY=
PORT=3001
# parse model — defaults to claude-haiku-4-5 (tested 6/6 on the spec messages)
PARSE_MODEL=
# hours before a pending pickup is flagged overdue (default 24; set low to demo)
PICKUP_WINDOW_HOURS=
